export default function Page() {
  return (
    <main className="page-shell">
      <section className="placeholder-card">
        <h1>HI-DORRI Backend Workspace</h1>
        <p>This branch keeps only the minimal frontend shell required by Next.js.</p>
      </section>
    </main>
  );
}
